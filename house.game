import random
import time
import pyttsx3

def house():
    numbers = [s for s in range (1, 91)]
    for i in range (90):
        n = random.choice(numbers)
        print(n)
        numbers.remove(n)
        #time.sleep(0.5)
    print("Game over")
#f= Timer(2, house)
#f.start()
house()
